import styled from 'styled-components'
import FImage from 'components/common/FImage/FImage'

export const ImagePreviewerLayout = styled(FImage)`
  width: inherit;
  aspect-ratio: 1/1;
  background-color: white;
  position: relative;

  cursor: pointer;
`

import styled from 'styled-components'
import { FImage } from 'components/common'

export const AuthTemplateLayout = styled.div`
  display: flex;
  width: 100%;
  height: 100vh;
`

export const Background = styled(FImage)`
  position: relative;
  flex: 1;
`

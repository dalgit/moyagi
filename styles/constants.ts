import { css } from 'styled-components'

export const baseHover = css`
  :hover {
    cursor: pointer;
    opacity: 0.6;
  }
`

export const quillModal = css`
  .quill {
    max-width: 400px;
  }

  .ql-editor {
    height: 300px;
  }
`

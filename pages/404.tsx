import { Layout, MainHeader, NotFoundTemplate } from 'components/Template'

const NotFoundPage = () => {
  return (
    <Layout>
      <MainHeader />
      <NotFoundTemplate />
    </Layout>
  )
}

export default NotFoundPage

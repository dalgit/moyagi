import styled from 'styled-components'
import { Button } from 'components/common'
import { quillModal } from 'styles/constants'

export const PostCreateFormLayout = styled.div`
  ${quillModal}
`

export const EventButton = styled(Button)`
  width: 100%;
`
